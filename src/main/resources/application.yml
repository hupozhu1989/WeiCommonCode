server:
  port: 8088 #服务端口

spring:
  application:
    name: wei-springboot #指定服务名
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password: 123456
#    timeout: 5000
    jedis:
      pool:
        max-idle: 8
        min-idle: 0
        max-active: 8
        max-wait: -1

